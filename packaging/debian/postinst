#!/bin/sh
set -e

if [ "${1:-}" = configure ]; then
  echo "Agent Root Broker package installed but the host is not configured."
  echo "Review the documentation, then run: sudo rootbroker-setup --profile grok --approver-user USER --agent-bin PATH"
fi

exit 0
